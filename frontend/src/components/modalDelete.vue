<template>
  <div class="modal-backdrop-data">
    <div class="modal-data">
      <header class="modal-header">
        <slot name="header">
          Are you sure to delete post ID:{{ data }}?
        </slot>
        <button
            type="button"
            class="btn-close"
            @click="$emit('close')"
        >
          x
        </button>
      </header>

      <footer class="modal-footer">
        <button
            type="button"
            class="btn btn-danger"
            @click="$emit('deletePost',data)"
        >
          Delete
        </button>
        <button
            type="button"
            class="btn btn-default"
            @click="$emit('close')"
        >
          Cancel
        </button>
      </footer>
    </div>
  </div>
</template>

<script>
export default {
  name: "modalDelete",
  props: ['data'],
}
</script>

<style scoped>

.modal-backdrop-data {
  position: fixed;
  top: 0;
  bottom: 0;
  left: 0;
  right: 0;
  background-color: rgba(0, 0, 0, 0.05);
  display: flex;
  justify-content: center;
  align-items: center;
}

.modal-data {
  background: #FFFFFF;
  overflow-x: auto;
  display: flex;
  flex-direction: column;
  width: 325px;
}

.modal-header,
.modal-footer {
  padding: 15px;
  display: flex;
}

.modal-header {
  position: relative;
  border-bottom: 1px solid #eeeeee;
  color: #4AAE9B;
  justify-content: space-between;
}

.modal-footer {
  border-top: 1px solid #eeeeee;
  flex-direction: row;
  justify-content: flex-end;
}

.modal-body {
  position: relative;
  padding: 20px 10px;
}

.btn-close {
  position: absolute;
  top: 0;
  right: 0;
  border: none;
  font-size: 20px;
  padding: 10px;
  cursor: pointer;
  font-weight: bold;
  color: #4AAE9B;
  background: transparent;
}

.btn-green {
  color: white;
  background: #4AAE9B;
  border: 1px solid #4AAE9B;
  border-radius: 2px;
  cursor: pointer;
}
</style>